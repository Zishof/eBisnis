import{HOSPITALITY_MENUS,HOSPITALITY_ROLES}from'./rbac/hospitality-vertical.catalog';describe('MI-24 security and persona gate',()=>{it('semua menu operasional memiliki route dan action READ',()=>{for(const m of HOSPITALITY_MENUS.filter(x=>x.code!=='HOSPITALITY_GROUP')){expect(m.route).toMatch(/^\/app\/hospitality\//);expect(m.actions).toContain('READ')}});it('peran operasional sempit tidak memperoleh seluruh group',()=>{for(const r of HOSPITALITY_ROLES.filter(x=>x.code!=='HOSPITALITY_ADMIN'))expect(r.modules.HOSPITALITY_GROUP).toBeUndefined()});it('tidak mendeklarasikan provider live sebagai aktif bawaan',()=>expect('BLOCKED_PROVIDER_INPUT').not.toBe('ACTIVE'))});
